import { SortedhashPipe } from './sortedhash.pipe';

describe('SortedhashPipe', () => {
  it('create an instance', () => {
    const pipe = new SortedhashPipe();
    expect(pipe).toBeTruthy();
  });
});
